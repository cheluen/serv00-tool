# Serv00 工具箱配置文件示例
# 复制此文件到 ~/.serv00-tool/config 并根据需要修改

# 默认编辑器
DEFAULT_EDITOR=nano

# 日志级别 (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

# 自动检查更新 (true/false)
AUTO_CHECK_UPDATE=true

# 显示颜色输出 (true/false)
ENABLE_COLORS=true

# 默认 screen 会话名称
DEFAULT_SCREEN_SESSION=main

# 备份目录
BACKUP_DIR=$HOME/backups

# 工具安装偏好
# 可选值: pkg, manual, ask
INSTALL_METHOD=pkg

# 进程监控刷新间隔（秒）
PROCESS_REFRESH_INTERVAL=5

# 最大日志文件大小（MB）
MAX_LOG_SIZE=10

# 是否在启动时显示系统信息 (true/false)
SHOW_SYSTEM_INFO_ON_START=false

# 自定义别名（用分号分隔）
CUSTOM_ALIASES="ll='ls -la';la='ls -A';..='cd ..'"

# 自定义 PATH 目录（用冒号分隔）
CUSTOM_PATH_DIRS="$HOME/bin:$HOME/.local/bin"

# 网络超时设置（秒）
NETWORK_TIMEOUT=30

# 是否启用实验性功能 (true/false)
ENABLE_EXPERIMENTAL=false
